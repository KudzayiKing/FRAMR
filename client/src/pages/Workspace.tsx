/** Design reference: the application workspace keeps the source's permanent role-based sidebar and compact operational header. */
import { useMemo, useState } from "react";
import { ArrowLeft, BarChart3, CreditCard, ExternalLink, Film, LayoutGrid, Layers3, Menu, Megaphone, Package, Plus, ScanSearch, Search, Settings, Store, Upload } from "lucide-react";
import { toast } from "sonner";
import { FramrMark } from "@/components/framr/FramrMark";
import { FramrButton, Chip } from "@/components/framr/FramrPrimitives";
import { AdvertiserWorkspace, CreatorWorkspace } from "@/components/framr/WorkspaceViews";
import { CampaignModal, ExportModal, GenerationModal, ProductModal, ReserveModal, UploadVideoModal } from "@/components/framr/WorkspaceModals";
import { IMG, initialAssets, initialCampaigns, initialVideos, listings, type Campaign, type MarketplaceListing, type ProductAsset, type Video } from "@/data/framr";

type Role = "creator" | "advertiser";
type WorkspaceProps = { role: Role; onExit: () => void };
type NavItem = { id: string; label: string; icon: typeof LayoutGrid };
const creatorNav: NavItem[] = [{ id: "home", label: "Home", icon: LayoutGrid }, { id: "videos", label: "Videos", icon: Film }, { id: "placements", label: "Placements", icon: ScanSearch }, { id: "versions", label: "Versions", icon: Layers3 }, { id: "market", label: "Marketplace", icon: Store }, { id: "campaigns", label: "Campaigns", icon: Megaphone }, { id: "analytics", label: "Analytics", icon: BarChart3 }, { id: "assets", label: "Assets", icon: Package }, { id: "settings", label: "Settings", icon: Settings }];
const advertiserNav: NavItem[] = [{ id: "overview", label: "Overview", icon: LayoutGrid }, { id: "campaigns", label: "Campaigns", icon: Megaphone }, { id: "placements", label: "Placements", icon: ScanSearch }, { id: "products", label: "Products", icon: Package }, { id: "market", label: "Marketplace", icon: Store }, { id: "analytics", label: "Analytics", icon: BarChart3 }, { id: "billing", label: "Billing", icon: CreditCard }, { id: "settings", label: "Settings", icon: Settings }];

export default function Workspace({ role, onExit }: WorkspaceProps) {
  const [page, setPage] = useState(role === "creator" ? "home" : "overview"); const [mobileOpen, setMobileOpen] = useState(false); const [search, setSearch] = useState("");
  const [videos, setVideos] = useState<Video[]>(initialVideos); const [assets, setAssets] = useState<ProductAsset[]>(initialAssets); const [campaigns, setCampaigns] = useState<Campaign[]>(initialCampaigns);
  const [modal, setModal] = useState<"upload" | "product" | "campaign" | "generate" | "export" | null>(null); const [reserve, setReserve] = useState<MarketplaceListing | null>(null); const [selectedVideoId, setSelectedVideoId] = useState("v1"); const [exportLabel, setExportLabel] = useState("Auris Model A");
  const nav = role === "creator" ? creatorNav : advertiserNav; const title = nav.find((item) => item.id === page)?.label ?? page;
  const selectedVideo = useMemo(() => videos.find((video) => video.id === selectedVideoId) ?? videos[0] ?? null, [videos, selectedVideoId]);
  const updateVersion = () => { if (!selectedVideo) return; setVideos((current) => current.map((video) => video.id === selectedVideo.id ? { ...video, versions: [...video.versions, { id: `version-${Date.now()}`, label: "Auris Model A", brand: "Auris", image: IMG.auris, active: false }] } : video)); setPage("versions"); };
  const addVideo = () => setVideos((current) => [{ id: `video-${Date.now()}`, title: "Fried Rice Final v3", thumbnail: IMG.original, duration: "00:34", status: "ready", views: "—", placements: [{ id: `placement-${Date.now()}`, object: "Rice cooker", category: "Kitchen appliances", start: "00:06", end: "00:18", duration: 11.8, quality: "Excellent", confidence: 94, box: { left: 45, top: 11, width: 49, height: 47 } }], versions: [{ id: `original-${Date.now()}`, label: "Original", brand: "Source", image: IMG.original, active: true, source: true }] }, ...current]);
  const contentProps = { page, videos, assets, campaigns, search, onPage: setPage, onSelectVideo: (videoId: string) => { setSelectedVideoId(videoId); setPage("placements"); }, onUpload: () => setModal("upload"), onProduct: () => setModal("product"), onCampaign: () => setModal("campaign"), onGenerate: () => setModal("generate"), onExport: (label: string) => { setExportLabel(label); setModal("export"); }, onReserve: (listing: MarketplaceListing) => setReserve(listing), onToggleVersion: (videoId: string, versionId: string) => setVideos((current) => current.map((video) => video.id !== videoId ? video : { ...video, versions: video.versions.map((version) => version.id === versionId ? { ...version, active: !version.active } : version) })), onToast: (message: string) => toast(message) };
  return <div className="min-h-screen bg-paper text-ink"><button aria-label="Close navigation" onClick={() => setMobileOpen(false)} className={`fixed inset-0 z-30 bg-night/50 lg:hidden ${mobileOpen ? "block" : "hidden"}`} /><aside className={`fixed inset-y-0 left-0 z-40 flex w-64 flex-col border-r border-line bg-paper transition-transform duration-300 lg:translate-x-0 ${mobileOpen ? "translate-x-0" : "-translate-x-full"}`}><div className="flex h-16 items-center gap-2 border-b border-line px-5"><button onClick={onExit} title="Back to home" aria-label="Back to home" className="-ml-1.5 p-1.5 text-inksoft transition hover:text-ink"><ArrowLeft size={18} /></button><FramrMark compact onNavigate={onExit} /><Chip className={`ml-auto ${role === "creator" ? "bg-paper2 text-inksoft" : "bg-accent-soft text-accent"}`}>{role.toUpperCase()}</Chip></div><nav className="flex-1 space-y-0.5 overflow-y-auto p-3">{nav.map(({ id, label, icon: Icon }, index) => <button key={id} onClick={() => { setPage(id); setMobileOpen(false); }} className={`nav-item ${page === id ? "nav-item--active" : ""} ${index === nav.length - 2 ? "mt-4 border-t border-line pt-4" : ""}`}><Icon size={16} />{label}{label === "Videos" && <span className="ml-auto text-[10px] font-bold">{videos.length}</span>}{label === "Campaigns" && role === "creator" && <span className="ml-auto text-[10px] font-bold text-accent">2</span>}</button>)}</nav><div className="border-t border-line p-3"><div className="flex items-center gap-3 p-2"><span className={`flex h-9 w-9 items-center justify-center rounded-full text-xs font-extrabold ${role === "creator" ? "bg-ink text-paper" : "bg-accent text-white"}`}>{role === "creator" ? "LK" : "AU"}</span><div className="min-w-0"><div className="truncate text-xs font-bold">{role === "creator" ? "Lena Kovač" : "Auris Home"}</div><div className="text-[10px] text-inksoft">{role === "creator" ? "@lena.cooks · Pro" : "Growth plan · Stripe"}</div></div><button className="ml-auto p-1.5 text-inksoft hover:text-ink" title="Back to site" onClick={onExit}><ExternalLink size={16} /></button></div></div></aside><div className="min-h-screen lg:pl-64"><header className="sticky top-0 z-20 flex h-16 items-center gap-3 border-b border-line bg-paper/95 px-5 backdrop-blur"><button className="-ml-2 p-2 lg:hidden" aria-label="Open menu" onClick={() => setMobileOpen(true)}><Menu size={20} /></button><h1 className="text-lg font-extrabold tracking-tight">{title}</h1><Chip className="hidden bg-paper2 text-inksoft sm:inline-flex">DEMO WORKSPACE</Chip><div className="relative ml-auto hidden md:block"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-inksoft" size={16} /><input value={search} onChange={(event) => setSearch(event.target.value)} className="h-9 w-56 rounded-md border border-line bg-white py-0 pl-9 pr-3 text-sm placeholder:text-inksoft/60" placeholder={role === "creator" ? "Search videos, placements…" : "Search marketplace…"} /></div><FramrButton size="sm" variant="accent" onClick={() => setModal(role === "creator" ? "upload" : "campaign")}>{role === "creator" ? <><Upload size={16} /><span className="hidden sm:inline">Upload video</span></> : <><Plus size={16} /><span className="hidden sm:inline">New campaign</span></>}</FramrButton></header><main className="mx-auto w-full max-w-[1400px] p-5 sm:p-8">{role === "creator" ? <CreatorWorkspace {...contentProps} /> : <AdvertiserWorkspace {...contentProps} />}</main></div><UploadVideoModal open={modal === "upload"} onClose={() => setModal(null)} onComplete={addVideo} /><ProductModal open={modal === "product"} onClose={() => setModal(null)} onSave={(asset) => setAssets((items) => [...items, asset])} /><CampaignModal open={modal === "campaign"} onClose={() => setModal(null)} onCreate={(campaign) => setCampaigns((items) => [campaign, ...items])} /><GenerationModal open={modal === "generate"} onClose={() => setModal(null)} video={selectedVideo} onFinish={updateVersion} /><ExportModal open={modal === "export"} onClose={() => setModal(null)} label={exportLabel} /><ReserveModal listing={reserve} open={Boolean(reserve)} onClose={() => setReserve(null)} /></div>;
}

